const dashTwixt2Evens = number => {
    let res = ""
    while(number > 9){
        let curr = number % 10;
        number = number / 10;
        let last = number % 10;
        if(last % 2 === 0 && curr % 10 === 0){
            
        }
    }
}